
import React, { Component } from 'react';

//This homepage will work as a Router for all the components and all the Paths so we determine what all paths we want and
// route it from this file.

class HomePage extends Component {
    render() {
        return (
            <div className="App">
                <header className="App-header">

                </header>
            </div>
        );
    }
}

export default HomePage;
